import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import {packageQuiz} from '../server/package-quiz.js';import {BUILTIN_QUIZZES} from '../server/builtins.js';import {GameRuntime} from '@manaty/retro-museum-sdk/runtime';
test('content packages physically exclude unselected starter banks and reject their IDs',async()=>{const base=JSON.parse(await readFile(new URL('../dist/game.rmg.json',import.meta.url)));const pack=packageQuiz(base,BUILTIN_QUIZZES.fruits);assert.ok(!pack.engine.includes(BUILTIN_QUIZZES.kings.questions[0].prompt.en));assert.ok(!pack.engine.includes(BUILTIN_QUIZZES.flags.questions[0].image));const runtime=new GameRuntime(pack,[{id:'a',number:1}]);try{assert.equal(runtime.snapshot().total,10);}finally{runtime.dispose();}assert.throws(()=>new GameRuntime(pack,[{id:'a',number:1}],null,{quizId:'kings'}),/Invalid quiz|Unknown quiz/);});
